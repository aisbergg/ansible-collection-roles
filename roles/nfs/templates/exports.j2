{{ ansible_managed | comment }}

#
# /etc/exports - see exports(5) for details
#

{% for export in nfs_exports %}
{{ '"' ~ export.path ~ '"' if ' ' in export.path|mandatory else export.path }} {{ export.options|join(' ') if export.options|mandatory is list else export.options }}
{% endfor %}
